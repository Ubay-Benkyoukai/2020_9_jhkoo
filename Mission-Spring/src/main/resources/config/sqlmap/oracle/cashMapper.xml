<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cash.dao.CashDAO">

	<!-- 현금 내역 추가 -->
  	<insert id="insert" parameterType="cashVO">
  		INSERT INTO cash_manage(id, cash, content)
  		 VALUES(#{id}, #{cash}, #{content})
  	</insert>
  	
  	<!-- 내역 리스트 조회 -->
  	<select id="selectAll" parameterType="String" resultType="cashVO">
  		SELECT no, id, cash, content ,
  			date_format(log_date,'%Y년 %m월 %d일(%H시 %i분)') as logDate
  		 FROM cash_manage
  		 WHERE id = #{id}
  		 ORDER BY log_date DESC
  	</select>
  	
  	<!-- 내역 삭제 -->
  	<delete id="delete" parameterType="int">
  		DELETE FROM cash_manage
  		 WHERE no =  #{no}
  	</delete>
  	
</mapper>