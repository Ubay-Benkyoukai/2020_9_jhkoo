<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reply.dao.ReplyDAO">

	<insert id="insert" parameterType="replyVO">
		INSERT INTO a_reply(board_no, content, writer)
		 VALUES(#{boardNo}, #{content}, #{writer})
	</insert>
  	
  	<!-- 게시물 번호의 해당 댓글 리스트 조회 -->
  	<select id="selectAll" parameterType="int" resultType="replyVO">
  		SELECT no, board_no as boardNo, content, writer, date_format(reg_date, '%Y-%m-%d') as regDate
  		 FROM a_reply
  		 WHERE board_no = #{boardNo}
  		 ORDER BY no DESC
  	</select>
  	
  	<!-- 댓글번호에 해당 댓글 삭제 -->
  	<delete id="delete" parameterType="int">
  		DELETE FROM a_reply
  		 WHERE no = #{no}
  	</delete>
  	
  	<select id="selectBoardNo" parameterType="int" resultType="int">
  		SELECT board_no
  		 FROM a_reply
  		 WHERE no = #{no}
  	</select>
  	
</mapper>